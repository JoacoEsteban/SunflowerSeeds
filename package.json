{
	"name": "sunflowerseeds",
	"displayName": "SunflowerSeeds",
	"description": "Automatically changes the Theme depending on the time of day.",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.33.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:extension.praiseTheSun"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "extension.praiseTheSun",
				"title": "SunflowerSeeds Enable"
			},
			{
				"command": "extension.showRemainingTime",
				"title": "SunflowerSeeds Show Remaining Time"
			}
		],

		"configuration": {
            "type": "object",
            "title": "SunflowerSeeds",
            "properties": 
            {

                "SunflowerSeeds.sunTime": 
                {
                    "type": "integer",
                    "default": 700,
                    "description": "Time at wich the *Day* theme will be set."
                },
                
                "SunflowerSeeds.moonTime": 
                {
                    "type": "integer",
                    "default": 1800,
                    "description": "Time at wich the *Night* theme will be set."
                },

                "SunflowerSeeds.themes": 
                {
                    "type": "array",
                    "default": [
                        "Visual Studio Light",
                        "Visual Studio Dark"
                    ],
                    "description": "List of themes to change periodically."
                }

            }
        }
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"postinstall": "node ./node_modules/vscode/bin/install",
		"test": "npm run compile && node ./node_modules/vscode/bin/test"
	},
	"devDependencies": {
		"typescript": "^3.3.1",
		"vscode": "^1.1.28",
		"tslint": "^5.12.1",
		"@types/node": "^10.12.21",
		"@types/mocha": "^2.2.42"
	}
}
